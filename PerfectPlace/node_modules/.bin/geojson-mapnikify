#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../geojson-mapnikify/bin/geojson-mapnikify" "$@"
  ret=$?
else 
  node  "$basedir/../geojson-mapnikify/bin/geojson-mapnikify" "$@"
  ret=$?
fi
exit $ret
